function expand( element ) {
	var target = document.getElementById(element);
	var h = target.effectHeight;
	var sh = target.srollHeight;
	var loopTimer = setTimeout();
	
	return x;
}
function ajaxReturn(x){
	if(x.readyState == 4 && x.status == 200){
	    return true;	
	}
}
